# The 0.8.x → 0.9.x run — Illness, random life, the audit, and the Nexus beta

**Status:** PLAN (2026-08-13, revised) — 0.8.0 shipped and verified
in-game; 0.8.1 in progress; 0.8.2–0.9.2c designed below, nothing
built. The author restructured the back half: a random-interactions
trial (0.8.5a), a scope audit (0.8.6a), and a dialog/animations/babies
run that ends in a **beta release on Nexus** (0.9.2c) instead of a
GitHub freeze.
**Related:** Roadmap.md (the milestone ledger), Illness.md (0.8.0's
design), ReleasePlan.md (the path to 1.0.0 — its cut list feeds the
0.8.6a audit), FutureStones.md, CoSave.md, Life.md (the player
window), RealEvents.md (talk/rows/fights — the random-interaction
raw material), the engine's AdapterProject.md (the boundary).

---

## The run, in build order

Each stone is verified in-game before the next — the standing rule.
The first four stones anchor the run in what shipped and what the
docs already slated; the back half is the author's restructure.

```
0.8.0  Illness & Medicine          ✅ shipped, verified in-game 2026-08-13
0.8.1  The Illness Field Pass      ◐ co-save audit landed; balance/cough/news/wound remain
0.8.2  The Burial                  — the settlement lays its own to rest
0.8.3  The Sick Household          — medicine as a stocked good; families care
0.8.4  MCM + Settings Manager      — the player tunes in-game
0.8.5a Random Interactions         — the trial: settlers interact unprompted; implement if it works
0.8.6a The Audit                   — what's missing, what's not needed
0.8.6b Redefine & Loose Ends       — apply the audit; close the seams
0.8.6c Scale in the Field          — the hard gate, measured
0.9.1a Dialog: more random lines   — the pools grow for the new interactions
0.9.1b Timings & Weights           — the cadence and probability, tuned
0.9.1c Babies, implemented         — the birth journey made visible/real
0.9.2a Animations                  — the interactions get bodies
0.9.2b Final Touches               — the clean run, docs, release materials
0.9.2c Beta on Nexus               — the first public release
```

Why this order: the field pass (0.8.1) closes the seams the shipped
stone exposed. The burial (0.8.2) and the sick household (0.8.3)
deepen the life/illness story. MCM (0.8.4) is the deferred tuning
convenience. **0.8.5a is a trial, not a promise** — the author wants
to look at settlers randomly interacting with each other, run test
trials, and only implement if it succeeds; if the engine's patterns
can't carry it, it degrades to a documented finding and the run
moves on. **0.8.6a is the honest audit** (ReleasePlan's cut list
revisited against what exists now); 0.8.6b applies it. 0.8.6c is the
hard gate. The 0.9.x run is content and polish toward the Nexus
beta.

## Engine needs audit — Request E: none

Every stone was checked against the engine's public surface. Nothing
new is needed; the run lives entirely at the adapter edge.

| Stone | Rides existing surface | New engine surface |
|-------|------------------------|--------------------|
| 0.8.1 Field pass | existing Illness stone | **none** |
| 0.8.2 Burial | game corpse refs, adapter-owned body state | **none** |
| 0.8.3 Sick household | shared wallet (Households), stall, market | **none** |
| 0.8.4 MCM | INI + a Papyrus surface (adapter side) | **none** |
| 0.8.5a Random interactions | existing Social/Trade channels, Say, proximity | **none** (verify the trial) |
| 0.8.6a/b Audit | — | **none** |
| 0.8.6c Scale | the engine's TickReport (read-only) | **none** |
| 0.9.1a/b Dialog | Dialogue.h pools + the seeded picker | **none** |
| 0.9.1c Babies | the birth lifecycle + the baby-mod bridge (0.7.8) | **none** |
| 0.9.2a Animations | the ESP/idle pattern from the kick (0.7.6) | **none** |
| 0.9.2b/c Final + beta | — | **none** |

---

## 0.8.1 — The Illness Field Pass

**Goal:** close the seams the natural-radstorm test exposed.

1. **Illness economy balance** — 25 caps per dose, the recovery
   window, and the medicine price vs typical pouch wealth. The day-12
   test showed 73 buys; confirm the market cure doesn't make illness
   a non-event, and that the broke-rest path is the honest minority.
2. **The cough at outbreak scale** — MTCoughing every 12 s per sick
   mind is a flood in a settlement-wide outbreak; verify the rate-
   limit holds and the tell stays audible without spamming.
3. **Illness news cadence** — "X is ill" once per sickness is the
   design, but a 50-sick outbreak must not swamp the radio; confirm
   the announce set holds at scale.
4. **Wound vector sanity** — an untreated wound is the only death
   that should be earned; verify the 40 s rescue window reads right
   in game (medicine still turns it around).

**Files:** `src/Tuning.h` + `config/TheLivingCommonwealth.ini` (any
knob the balance finds), `src/Adapter.cpp` (the cough/news throttles).

**Verification:** a forced radstorm day (or a natural one — the
Commonwealth obliges): the sick cough at a sane cadence, the radio
announces without flooding, the broke-rest path appears, and a wound
victim with caps survives. Mid-outbreak save/load already locks.

## 0.8.2 — The Burial

**Goal:** the corpse must not linger forever (ADR-0023's planned
stone). The sim's death path is complete in the books — death fact,
grief, legacy — but the game corpse stays in the settlement cell
forever (no cell reset there). After the mourning window
(`sim.death.burialDays`, INI-tunable), the adapter disables the
corpse ref and logs `the settlement laid X to rest` (+ a news line).
Body state is game state — adapter-owned, no engine change.

**Files:** `src/Adapter.cpp` (the burial sweep), `src/Tuning.h` +
the INI (`sim.death.burialDays`).

**Verification:** kill a settler, wait out the window, the body is
gone and the settlement heard it. Save/load across the burial: a
buried corpse stays buried.

## 0.8.3 — The Sick Household

**Goal:** the economy stone's next step — medicine is not an endless
shelf, and a family cares for its own.

1. **Stocked medicine.** Stalls stock a limited supply per day
   (`sim.illness.stock`, INI) that sells out, so an outbreak can
   outrun the market — the broke-rest path and the honest season
   return. Replenishes the next market day.
2. **The family buys for the sick.** The shared wallet already
   exists; when one spouse is ill, the other's market trip can buy
   the dose for the sick one — a care check on the household.

**Files:** the stall/trade path, `Households` (the care check),
`src/Tuning.h` + the INI (`sim.illness.stock`).

**Verification:** a big outbreak drains the stalls and the sick wait
for tomorrow; a married sick settler gets dosed by the spouse's trip
to the bench.

## 0.8.4 — MCM + Settings Manager

**Goal:** the player tunes in-game (Life.md's deferred player-window
half). A Mod Configuration Menu page exposing the tuning keys —
hunger rhythm, market hours, bond thresholds, births gate, illness
curve, gossip radius, population cap. The INI stays the source of
truth; MCM reads and writes it (or an override file), with a
restore-defaults button. The MCM mod is a **soft** dependency; a
small Papyrus surface on the adapter side.

**Files:** a Papyrus/MCM page (author-side asset), the adapter's
settings surface it calls.

**Verification:** an MCM change survives reload; defaults restore
cleanly; with no MCM installed, the INI alone still works.

## 0.8.5a — Random Interactions (the trial)

**Goal:** settlers interact with each other unprompted — not only
when hunger drives them to a bench. The trial first: watch what the
existing sim already produces (trades at person-markets, rows at the
bench, the Socialize intent, proximity), run test trials, and
implement only what succeeds. The engine's patterns (Social/Trade
channels, `Say`, proximity reads) are the raw material; if the trial
shows the sim can't carry unprompted interaction cleanly, the stone
degrades to a documented finding and the run moves on.

**Files:** `src/Adapter.cpp` (the interaction pass), `Dialogue.h`
(the pools it speaks), `src/Tuning.h` + the INI (the cadence and
weights — ahead of 0.9.1a/b's tuning).

**Verification:** two settlers who cross paths sometimes greet, talk,
or trade without a hunger drive; the interaction is rate-limited,
never spammy, and doesn't interrupt a walk. If the trial fails, the
finding is written and the stone is closed as a cut.

## 0.8.6a — The Audit

**Goal:** what's missing, what's not needed. ReleasePlan's cut list
revisited against what now exists: the hands pillar (build/move/
destroy), pex scenes, MCM (built in 0.8.4 — re-assess), audio radio,
provisioner naming, and anything the field tests flagged as noise.
Each item is a decision, not an accident.

## 0.8.6b — Redefine & Loose Ends

**Goal:** apply the audit. Scope is redefined to what the beta
actually needs; loose ends from 0.8.1–0.8.5a are closed; the docs
(ReleasePlan, Roadmap, Run080) are reconciled with the cuts.

## 0.8.6c — Scale in the Field

**Goal:** the hard gate, measured. The engine proved 5000 minds
round-trip and year-long soaks; the adapter must prove a normal save
with hundreds of settlers ticks inside a frame budget. The engine's
`TickReport` (once a minute) measures it. A beta that chugs at 600
minds is dead on arrival.

**Files:** the tick path, whatever the report names.

**Verification:** a restored 600+ mind save holds frame time over a
long session; any hotspot found is fixed or gated behind a knob.

## 0.9.1a — Dialog: more random lines

**Goal:** the pools grow for the interactions 0.8.5a proved. New
greets, gossip, trades, and banter lines — the author's style: the
good, the bad, and the ugly — feeding the seeded picker. No new
machinery; the pools are data.

## 0.9.1b — Timings & Weights

**Goal:** the new interactions' cadence and probability, tuned in
INI (`sim.interact.*`): how often a crossing becomes a greeting, a
chat, a row, or a trade; the weights against the existing intents;
the per-day and per-mind rate limits so the show stays a show.

## 0.9.1c — Babies, implemented

**Goal:** the birth journey made visible/real. The sim-side lifecycle
already exists (0.7.7) and the actor bridge pairs grown children to
game actors (0.7.8). This stone is the full implementation — the
baby-mod integration as a soft/optional dependency (permission
already requested), bottles/cribs as walk targets, the market selling
baby goods, and the whole journey co-save-safe.

## 0.9.2a — Animations

**Goal:** the interactions get bodies. The ESP/idle pattern proven by
the kick (0.7.6, `TheLivingCommonwealthAnims.esp`) extends to the new
interactions: a greeting gesture, a chat stance, the altercation's
shove — unconditional IDLE clones, resolved load-order-independently,
with graceful fallback when the ESP is absent.

## 0.9.2b — Final Touches

**Goal:** the clean run before the beta. Every field note folded in,
the docs reconciled, warnings cleared, the harness at full green, the
README written for players (what it does, install, tune, known
limits, compatibility), and the release package assembled.

## 0.9.2c — Beta on Nexus

**Goal:** the first public release. The beta package ships on Nexus
with the release description and changelog; the GitHub repo stays the
source; feedback from the beta feeds the post-beta run.

---

## The hand-off to the engine tab

One paragraph, for the record: **the adapter's 0.8.x–0.9.x run needs
no new engine surface.** Every stone rides the existing contract —
the shared wallet, the co-save additive record, the news feed, the
Say/talk channels, the engine's read-only TickReport. The only
cross-repo touchpoints are read-only. No Request E.
